program x2 
private class x3 : x4 {

    int def fib(int max) {
        var past, past_past, curr : int

        
        past = 1    
        past_past = 0
        curr = 0
        while(curr < max) {
            print(curr)
            curr = past + past_past
            past_past = past
            past = curr
        }

        return 1
    }

    void def main() {
        fib(39)
    }
}