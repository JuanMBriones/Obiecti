program arrFind
public class arr {
    void def main() {
        var arr[6] : int

        var index : int
        var temp : int
        index = 0
        while(index < 6) {
            read(temp)
            arr[index] = temp

            index = index + 1
        }

        print("Printing elements")
        index = index - 1
        while(index >= 0) {
            temp = index
            print(arr[index])
            index = index - 1
        }
    }
}
